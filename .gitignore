__pycache__
temp
.ruff_cache/
favorites.json

node_modules/
# web/ is the built frontend — committed on version bumps (per project convention)

backup

# Packaging artifact from the registry pack step — regenerated, never source.
node.zip
